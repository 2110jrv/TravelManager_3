import {platformRepository} from '../services/localPlatform.js';import {patchAppState} from '../app/appState.js';
export async function handleRemoteCommand(command){const deviceId=localStorage.getItem('agenda-viajera.device');const repo=await platformRepository();if(command.command==='PURGE_DOCUMENTS'||command.command==='REVOKE_AND_PURGE')await repo.purgeDocuments(deviceId);if(['BLOCK','REVOKE','REVOKE_AND_PURGE'].includes(command.command))localStorage.setItem('agenda-viajera.deviceState',command.command==='BLOCK'?'BLOCKED':'REVOKED');if(command.command==='CLOSE_SESSION'||['BLOCK','REVOKE','REVOKE_AND_PURGE'].includes(command.command))localStorage.removeItem('agenda-viajera.user');patchAppState({device:{deviceId,state:localStorage.getItem('agenda-viajera.deviceState')},documents:[]});return command;}

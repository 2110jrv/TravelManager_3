export function renderBudget(d,allowed=true){if(!allowed)return `<section class="empty"><h2>Presupuesto</h2><p>Tu rol no permite ver el presupuesto</p></section>`;const active=[...d.items,...(d.expenses||[])].filter(x=>x.status!=='IDEA'&&x.refundStatus!=='REFUNDED'),total=active.reduce((s,x)=>s+Number(x.amount||0),0),paid=active.reduce((s,x)=>s+Number(x.paid||0),0),estimated=active.filter(x=>x.costType==='ESTIMATED'||x.payment==='ESTIMATED').reduce((s,x)=>s+Number(x.amount||0),0),unknown=active.filter(x=>x.costType==='UNKNOWN').length;return `<section class="section-head"><div><span class="eyebrow">DERIVADO DE ITEMS ACTIVOS</span><h2>Presupuesto</h2></div></section><div class="budget-grid"><article class="metric"><small>Planificado</small><strong>$${total.toFixed(2)}</strong></article><article class="metric paid"><small>Pagado</small><strong>$${paid.toFixed(2)}</strong></article><article class="metric"><small>Pendiente exacto</small><strong>$${(total-paid-estimated).toFixed(2)}</strong></article></div><article class="card"><strong>Estimado</strong><span>≈ $${estimated.toFixed(2)}</span></article><article class="card"><strong>Desconocido</strong><span>${unknown} items</span></article>`}

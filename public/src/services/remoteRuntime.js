import {MockRemoteSyncAdapter} from './remoteSyncAdapter.js';import {handleRemoteCommand} from '../controllers/remoteCommandController.js';import {platformRepository} from './localPlatform.js';import {appState,patchAppState} from '../app/appState.js';import {flushPendingOperations} from '../controllers/mutationController.js';
export const REMOTE_MODE='mock';export const MOCK_REMOTE_URL='http://127.0.0.1:5055';let adapter=null;let timer=null;
export async function initRemoteRuntime(){if(REMOTE_MODE!=='mock')return{mode:'none',available:false};adapter=new MockRemoteSyncAdapter(MOCK_REMOTE_URL);const deviceId=localStorage.getItem('agenda-viajera.device');try{await adapter.registerDevice({deviceId,state:localStorage.getItem('agenda-viajera.deviceState')||'TRUSTED',name:'Agenda Viajera device'});await processDeviceCommands();await pullRemoteChanges();window.dispatchEvent(new CustomEvent('tm3-remote-state',{detail:{online:true,mode:REMOTE_MODE}}));timer=setInterval(processDeviceCommands,3000);return{mode:REMOTE_MODE,available:true}}catch(error){console.warn('Remote mock unavailable; local mode',error.message);window.dispatchEvent(new CustomEvent('tm3-remote-state',{detail:{online:false,mode:'none'}}));return{mode:'none',available:false}}}
export function getRemoteAdapter(){return adapter}export async function syncNow(){if(!adapter)return{available:false};await processDeviceCommands();await flushPendingOperations();await pullRemoteChanges();return{available:true}}
export async function flushPending(){return flushPendingOperations()}
export async function pullRemoteChanges(){const active=adapter||new MockRemoteSyncAdapter(MOCK_REMOTE_URL),repo=await platformRepository(),tripId='trip-italy-2026',records=await active.pullRecords({tripId,sinceVersion:0});for(const record of records)await repo.applyCanonical(record);if(records.length){const root=await repo.read('agendaEntries','root');patchAppState({agenda:root?.data?.items||[],ideas:root?.data?.ideas||[]});window.dispatchEvent(new CustomEvent('tm3-canonical-pulled'))}return records}
export async function processDeviceCommands(){if(!adapter)return[];const deviceId=localStorage.getItem('agenda-viajera.device');try{const commands=await adapter.getDeviceCommands(deviceId);for(const command of commands.filter(x=>x.status==='PENDING')){await handleRemoteCommand(command);await adapter.completeDeviceCommand(command.id)}const remote=await adapter.getDevice(deviceId);if(remote?.state==='REVOKED'||remote?.state==='BLOCKED'){localStorage.setItem('agenda-viajera.deviceState',remote.state);window.dispatchEvent(new CustomEvent('tm3-device-locked',{detail:remote}))}return commands}catch(error){return[]}}
export async function stopRemoteRuntime(){if(timer)clearInterval(timer);timer=null;adapter=null}
setTimeout(initRemoteRuntime,0);

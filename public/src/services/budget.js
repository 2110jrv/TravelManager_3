export function calculateTripBudget(tripId,items=[]){const rows=items.filter(x=>(x.tripId||tripId)===tripId&&x.status!=='IDEA'&&x.refundStatus!=='REFUNDED');const out={plannedExact:0,paid:0,pendingExact:0,estimated:0,unknownCount:0,cancelledNotRefunded:0,cancelledPending:0,byCategory:{},byCurrency:{}};for(const x of rows){const amount=Number(x.amount||0),currency=x.currency||'USD';if(x.costType==='ESTIMATED')out.estimated+=amount;else if(x.costType==='UNKNOWN')out.unknownCount++;else out.plannedExact+=amount;if(x.paymentStatus==='PAID'||x.payment==='PAID')out.paid+=Number(x.paid??amount);if(x.status==='CANCELLED'&&x.refundStatus==='NOT_REFUNDED')out.cancelledNotRefunded+=amount;if(x.status==='CANCELLED'&&x.refundStatus==='PENDING')out.cancelledPending+=amount;out.byCategory[x.type||'OTHER']=(out.byCategory[x.type||'OTHER']||0)+amount;out.byCurrency[currency]=(out.byCurrency[currency]||0)+amount;}out.pendingExact=out.plannedExact-out.paid;return out;}
